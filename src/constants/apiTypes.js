/**
 * 常量放在单独的文件中，合作者对整个 app 包含的 mutation 一目了然；
 * 同时可以用于api接口的调用
 */

export const LOGIN = 'user/login'; //登录
export const LOGOUT = 'user/logout'; //退出
export const GET_USER_INFO = 'user/getUserInfo'; //获取用户信息
